import strategy1Service from '../services/strategy1';

describe('(api call)', () => {
    it('should return an error if the endpoint does not return 200', () => {
        // ARRANGE

        // ACT

        // ASSERT
    });

    it('should return an error if the city is not found', () => {
        // ARRANGE

        // ACT

        // ASSERT
    });

    it('should return a forecast table if a valid forecast is returned', () => {
        // ARRANGE

        // ACT

        // ASSERT
    });
});
